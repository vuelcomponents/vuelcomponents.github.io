import type { CellClickedEvent } from 'ag-grid-community';

export default (params: CellClickedEvent, options: any) => {
  // if (params.colDef.field === 'chooseThisTransport') {
  //     if(options.service){
  //         options.service.setTransport(params.data.transport_id).then(res=>{
  //             if(res.status === 200){
  //                 if(options.router){
  //                     options.router.push({name: 'pick'})
  //                 }
  //             }
  //         })
  //     }
  // }
};
